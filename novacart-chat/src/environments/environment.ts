/**
 * Frontend config. Replace `apiUrl` with the URL of your deployed
 * inference API Gateway endpoint (see `novacart-pkg/docs/SETUP.md`).
 *
 * Example: 'https://<api-id>.execute-api.us-east-1.amazonaws.com/chat'
 */
export const environment = {
  production: false,
  apiUrl: 'https://YOUR-API-ID.execute-api.us-east-1.amazonaws.com/chat',
};
